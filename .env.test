# Vitest (jsdom) ortamı — relative fetch URL'leri Node'un native fetch'i
# çözemediği için mutlak bir base gerekiyor; jsdom'un varsayılan location'ıyla
# (http://localhost:3000) eşleşmeli ki MSW node server aynı origin'de eşlesin.
VITE_API_BASE_URL=http://localhost:3000
